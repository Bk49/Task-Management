import { Typography } from "@mui/material";

const App = () => {
    return (
        <Typography variant="h1" sx={{ fontSize: "2rem", fontWeight:"bold" }}>
            Task Management App
        </Typography>
    );
};

export default App;
